---
context: default
phase: ad-hoc-ux-bugfixes
task: null
total_tasks: null
status: paused
last_updated: 2026-06-06T13:46:10Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

_Nenhuma constraint bloqueante identificada nesta sessão._

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| dist não buildado | Coolify só faz build do Go. Mudanças React não aparecem em produção sem `npm run build` + `git add -f frontend/dist/` + commit + push | blocking | Sempre rodar `cd frontend && npm run build` antes do commit final de mudanças de frontend |

<current_state>
Sessão de ajustes visuais no Painel de Vendas (FarolV2Dashboard) e bugfix de auth no import.

Último commit pushado: `6070c30` — dist/ rebuilado e commitado.
Produção (farol.fbtax.cloud) deve estar servindo o novo layout após o Coolify reiniciar o container.

Layout implementado (aguardando validação visual em prod):
- Barra de controles: [Filial ▼] [Atual 01/mm/aaaa a 31/mm/aaaa × Ant. 01/mm/aaaa a 31/mm/aaaa ▼] [Por Indústria | Por Gerência | Por Equipe] [Ano a Ano | Projeção Anual | Mês a Mês] [+ Importar]
- KPIBar topo-esquerdo: [Mês corrente | Mês fechado] (substituem o texto periodo.label)
- KPIBar grid: Total Anterior | Total Atual | Atingimento | Faturado | Transmitido | Positivação | Mix médio
</current_state>

<completed_work>

- **FilialSelector** resestilizado: `h-8 rounded-lg border-slate-200 bg-white` — igual aos outros filtros
- **PeriodRangeFilter** (novo componente): chip clicável que mostra `Atual dd/mm/aaaa a dd/mm/aaaa × Ant. dd/mm/aaaa a dd/mm/aaaa`, abre Popover com selects de Base Atual e Base Anterior (Base Anterior editável só no modo Mês a Mês)
- **KPIBar**: removido `{periodo.label}` texto estático; adicionado [Mês corrente | Mês fechado] no topo esquerdo com lógica de preset (corrente = último período disponível, fechado = penúltimo)
- **Barra de controles**: filtros de dados (Filial + PeriodRange) vêm primeiro, depois view tabs e comparison mode
- **fix import 401**: todos os fetches de FarolV2Import agora enviam `Authorization: Bearer {token}` — polling de job, upload, cancel, refresh-views e listagem de períodos
- **dist rebuilado e commitado**: descoberto que Coolify não faz build do React — dist/ precisa ser commitado manualmente
</completed_work>

<remaining_work>

- Validar layout em produção após deploy do commit `6070c30`
- Importar os 12 meses de 2025 (test-data/*.csv) pelo painel para testar os novos filtros com dados reais
- Continuar ajustes visuais conforme feedback do usuário após ver o novo layout
</remaining_work>

<decisions_made>

- **dist/ buildado localmente**: Coolify serve estático do repo, não executa `npm run build`. A cada mudança de frontend: `cd frontend && npm run build && cd .. && git add -f frontend/dist/ && git commit && git push`
- **PeriodRangeFilter como Popover**: chip leve na barra de filtros, edição via popover para não sobrecarregar a barra
- **autoCompPeriodo**: yoy/ytd → refAno-1, mom sem override → mês anterior. Para mom, usuário pode sobrescrever a Base Anterior no popover
- **Mês corrente/fechado**: corrente = periodos[length-1], fechado = periodos[length-2]
</decisions_made>

<blockers>
- Nenhum bloqueante. Aguardando apenas o deploy do Coolify (automático após push).
</blockers>

## Required Reading (in order)
1. `.planning/STATE.md` — estado geral do projeto
2. `/home/claudio/.claude/projects/-home-claudio-projetos-FB-FAROL/memory/MEMORY.md` — memórias do projeto
3. `frontend/src/pages/farol/FarolV2Dashboard.tsx` — PeriodRangeFilter e KPIBar modificados
4. `frontend/src/components/FilialSelector.tsx` — trigger resestilizado

## Infrastructure State
- **Backend**: Go 8087, rodando localmente via `go run ./backend/...`
- **Frontend dev**: React 3087, `npm run dev` em `frontend/`
- **Produção**: farol.fbtax.cloud via Coolify — dist/ servido como estático do repo Go
- **Build workflow**: `cd frontend && npm run build` → `git add -f frontend/dist/` → commit → push
- **test-data/**: 12 CSVs de 2025 gerados localmente, NÃO commitados (são dados de teste)

<context>
Sessão foi de ajustes visuais no Painel de Vendas + bugfix crítico de auth no import (401 em produção).

A descoberta mais importante da sessão: o Coolify não builda o React. O dist/ está no .gitignore mas precisa ser commitado com `git add -f`. Isso tem que ser feito TODA vez que mudar código frontend.

O usuário está testando em localhost e em produção em paralelo enquanto importa 12 meses de dados de 2025.
</context>

<next_action>
1. Confirmar em produção (farol.fbtax.cloud) que o novo layout do Painel de Vendas está correto:
   - Barra de filtros: [Filial][PeriodRange chip][view tabs][comp mode]
   - KPIBar: [Mês corrente|Mês fechado] no topo esquerdo
2. Se ainda incorreto, rodar localmente (`npm run dev`) para confirmar que o código está certo
3. Após validação visual, importar test-data/*.csv para popular os dados de 2025
</next_action>
